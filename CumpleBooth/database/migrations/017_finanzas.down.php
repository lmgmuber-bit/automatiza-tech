<?php
/** Revierte la 017. No borra la tabla: adentro está lo que costó cada cosa, y eso no se
 *  puede volver a averiguar mirando el código. Si de verdad hay que borrarla, se hace a mano. */
return static function (PDO $pdo): void {
    // Sin operación a propósito.
};
