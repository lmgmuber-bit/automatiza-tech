ticket_id: AT-CUMPLECLICK-015
parent_ticket: AT-CUMPLECLICK-014
title: "Tu Cumple en 3D — Fase 4: integración CumpleClick (fotos y fiesta)"
origin: direct_luis
priority: P1
status: WAITING_HUMAN_GATE
created_at: "2026-09-06"
created_by: claude
objective: >
  Cerrar la fase 4 de "Tu Cumple en 3D": el juego lee la fiesta real de CumpleClick por
  `?p=<slug>` (nombre del festejado, invitados, marco y temática), saca fotos de recuerdo
  compuestas igual que el kiosco y las sube a la galería de la fiesta (`upload.php`), y cuelga
  las fotos del kiosco dentro del mundo 3D (cartel de la plaza, cuadros del salón, nombres
  sobre los globos) leyéndolas con el PIN de la galería por el nuevo `sala.php?op=fotos`.
ownership:
  planner: claude
  executor: claude
  reviewer: claude
  final_approver: "Luis Miguel"
  current_owner: claude
routing:
  complexity: C2
  risk: low
  uncertainty: low
  reversibility: high
scope:
  changed_files:
    # CumpleBooth vive dentro del repo automatiza-tech; rutas relativas a ese repo.
    - "CumpleBooth/public/sala.php"
    - "CumpleBooth/public/lib.sala.php"
    - "CumpleBooth/tests/backend/salas.php"
    - "CumpleBooth/README.md"
    # El juego vive en un repo git APARTE (C:\wamp64\www\tucumple-repo\), no dentro de
    # automatiza-tech; rutas relativas a ese repo.
    - "tucumple-repo/app/public/game/fiesta.js"
    - "tucumple-repo/app/public/game/foto.js"
    - "tucumple-repo/app/public/game/galeria.js"
    - "tucumple-repo/app/public/game/main.js"
    - "tucumple-repo/app/README.md"
    - "tucumple-repo/app/design/sala-api.md"
safety:
  gallery_pin_check: "cb_sala_fotos() exige cb_verify_party_pin(party, pin) antes de listar fotos"
  rate_limit: "op=fotos: 5/min por IP con bloqueo de 5 min (sala.php); upload.php: 30 subidas/10 min por IP, límite compartido con el kiosco"
  no_storage_key_or_raw_tokens_in_responses: true
  storage: "BD CumpleClick (cc_salas, cc_sala_ayudantes, cc_sala_acciones, cc_photos, cc_parties)"
  higgsfield_credits: 0
  allow_deploy: false
  allow_commit: true # solo en ramas propias
  allow_push: false # PROD va por FTP; lo sube Luis
execution_log:
  - date: "2026-09-06"
    by: claude
    what: >
      Lado CumpleClick: `cb_sala_fotos()` en `public/lib.sala.php` valida sala + anfitrión, slug
      de fiesta y PIN de la galería (`cb_verify_party_pin`) antes de devolver hasta 12 fotos
      recientes como rutas `ver.php?t=…&download=inline` (nunca tokens sueltos ni storage_key);
      nueva op `fotos` en `public/sala.php` con su propio límite (5/min, bloqueo 5 min).
      `cb_sala_ips_lan()` añade `hosts` (IPv4 privadas del servidor) a la salida de `crear`, para
      que el QR de la fase 3 sea alcanzable aunque la tablet abra el juego en `localhost`.
      `tests/backend/salas.php` subió de 61 a 73 checks. `upload.php` y `api.php` no se
      tocaron. Lado del juego (`tucumple-repo`, commit `0ec25f7`): `game/fiesta.js` lee
      `api.php?p=<slug>` (nombre, invitados, frameBox, temática) con base configurable por
      `?cc=` y recordada en `localStorage`; `game/foto.js` agrega un botón de foto manual y
      fotos automáticas (45 % de cada cinemática, 2,6 s en la fiesta final) que, con fiesta,
      componen un recuerdo 1080×1920 igual que el kiosco y lo suben a `upload.php`;
      `game/galeria.js` cuelga las fotos del kiosco y las del propio juego en un cartel de la
      plaza y en cuadros del salón del castillo, y pone los nombres de los invitados sobre los
      globos. Verificado con Playwright (WebGPU, 41–48 fps): fiesta de prueba `prueba-3d`
      cargada (Isidora, tema Reino de Hielo, 5 invitados), PIN de galería aceptado, foto manual
      PNG 1080×1920 de 2,9 MB subida y visible en la galería, listado con PIN devuelve la foto,
      cuadros del salón y cartel de la plaza la muestran, 5 nombres sobre los globos, foto
      automática de una cinemática subida sola, 0 errores de consola. Créditos de Higgsfield
      gastados: 0. Documentación sincronizada: `tucumple-repo/app/README.md` (estado + sección
      Fase 4), `tucumple-repo/app/design/sala-api.md` (op `fotos`, campo `hosts`, contrato de
      `upload.php`/`api.php?p=`), `CumpleBooth/README.md` (línea de `sala.php`/`lib.sala.php`).
      Nada desplegado a PROD; sin commit ni push de este ticket.
next_action: >
  Luis prueba con la fiesta real y un celular real en la misma Wi-Fi (abriendo la tablet por su
  IP de LAN para que el QR de ayudantes sea alcanzable). Después decide el FTP a PROD
  (`dist/sala.php`, `dist/lib.sala.php`) y si borra la fiesta de prueba `prueba-3d` de la BD
  local, si no la quiere conservar.
