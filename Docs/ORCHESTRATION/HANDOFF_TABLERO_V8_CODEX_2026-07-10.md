🧩 AutomatizaTech — Takeover y cierre técnico del tablero v8 (10 julio 2026)
Progress: Luis reasignó `AT-TAB-ARCH-001` de OpenCode a Codex. En `codex/finish-tablero-v8`, Codex corrigió CSP/CORS, autenticación Hostinger con `X-AT-Board-Token`, eliminación de token por URL, doble carga de secretos, setup `dbDelta`, mapeo camelCase, estados legacy, POST completos para drag/edición y modo offline de solo lectura. Se eliminó el script temporal con contraseña. Validaciones: PHP lint; preflight 204; GET 6 clientes/14 internas; POST cliente/interna; query token 401; Playwright headless con GET, edición, drag, rollback offline y cero page errors.
Plans: La implementación local está lista para despliegue controlado. Con autorización explícita de Luis: respaldar, subir `api-tablero.php`, `tablero/index.html` y `.htaccess` corregido, ejecutar/confirmar setup si corresponde, probar local↔PROD y eliminar `setup-at-board.php` del hosting. Después marcar el ticket DONE y liberar locks.
Problems: PROD todavía ejecuta los archivos anteriores; por eso no puede considerarse verificada la sincronización productiva. Las tablas v8 son una fuente operativa transitoria: integrar clientes canónicos, GitHub Issues y lease sigue como evolución. No hay autorización actual de deploy, merge ni push.
